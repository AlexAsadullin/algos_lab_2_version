на графиках видно, что:
1) для 2 укзаателей все датасеты +- одинаковы по времени (память тоже одинаковая, помехи на графике это фоновые процессы ОС)
2) для бинпоиска 2-й датасет чуть дольше отработал
3) для экспоненциального поиска датаеты 2 отработал немного дольше
4) для бинпоиска с разделением 2 и 3 датасеты сработали дольше первого

Вывод: худший случай: короткий массив - чтеные числа, длинный массив - нечетные числа (2-й датасет)

Далее провел эксперимент, насколько сильной должна быт разница в массивах: 
1) сначала запустил тесты с длиной: короткий = 5 млн, длинный = 10 млн, результат в папке "charts 5m vs 10m/"
2) затем запустил с длиной: короткий = 1 млн, длинный = 10 млн, результат в папке "charts/", разница во времени несного усилилась (видно на экспоненциальном больше всего)

Значит, чем больше разрыв между коротким и длинным, тем дольше будет отрабатывать 2 случай

**Вывод: худший случай: короткий массив - чтеные числа, длинный массив - нечетные числа (2-й датасет), разница в длине как можно больше (в 10 раз и более)**